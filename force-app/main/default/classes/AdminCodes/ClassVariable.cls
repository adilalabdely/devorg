public class VariabaleAssignment {
    public void variabaleAssignmentMethod(){
        String batchName ='Batch 16';
        Integer noOfStudents = 50;
        string description = 'this is the best batch and we are doing great';
        
        system.debug(batchName + ' has ' + noOfStudents + ' and ' + description );
              
    }
}